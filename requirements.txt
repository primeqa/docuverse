# DEPRECATED: dependencies have moved to pyproject.toml.
# Keep this file as a thin compatibility shim for external CI / Dockerfiles
# that pin against `pip install -r requirements.txt`. Prefer:
#   pip install -e .
-e .
